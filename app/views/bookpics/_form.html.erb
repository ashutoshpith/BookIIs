<%= form_with(model: bookpic, local: true) do |form| %>
  <% if bookpic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bookpic.errors.count, "error") %> prohibited this bookpic from being saved:</h2>

      <ul>
      <% bookpic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
